name: build forge
on:
  pull_request:
    branches: [ "**" ]
    paths:
      - .github/workflows/build-forge.yaml
      - .github/scripts/**
      - build.gradle
      - settings.gradle
      - gradle.properties
      - gradle/**
      - buildSrc/**
      - common/**
      - forge/**
  push:
    branches: [ "main" ]
    paths:
      - .github/workflows/build-forge.yaml
      - .github/scripts/**
      - build.gradle
      - settings.gradle
      - gradle.properties
      - gradle/**
      - buildSrc/**
      - common/**
      - forge/**

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: ${{ github.ref != 'refs/heads/main' }}
      - name: Gradle Build
        run: ./gradlew :forge:build
      - name: Install virtual display
        if: ${{ runner.os == 'Linux' }}
        run: sudo apt-get update && sudo apt-get install -y xvfb
      - name: Forge client smoke test
        if: ${{ runner.os == 'Linux' }}
        run: bash .github/scripts/smoke-run.sh forge client
      - name: Forge server smoke test
        if: ${{ runner.os == 'Linux' }}
        run: bash .github/scripts/smoke-run.sh forge server
      - name: capture build artifacts
        if: ${{ runner.os == 'Linux' }}
        uses: actions/upload-artifact@v4
        with:
          name: Artifacts
          path: forge/build/libs/
